# /bootstrap — Scaffold the repo

You are the agent. Do the following:

1) Create an Electron desktop app using Electron Forge + Vite + TypeScript template.
2) Add React for renderer UI.
3) Ensure Electron security baseline:
   - nodeIntegration false
   - contextIsolation true
   - preload bridge only
4) Produce a short RUN.md with:
   - install
   - dev run
   - test run (placeholder if not yet)
5) DO NOT implement AI or storage yet.

Deliverables:
- Working app window
- A simple UI with: "Inbox" header + "New Note" textarea + "Save" button (in-memory).
